import type { OperationContext } from '@core/orchestration/index.js';

/**
 * {{pascalCase name}} Service Types (Calculation)
 *
 * Use calculation services for compute-heavy operations like:
 * - Score calculation
 * - Data transformation
 * - Analysis pipelines
 *
 * Generated by: npm run generate
 */

/**
 * Policy configuration for {{camelCase name}}
 */
export interface {{pascalCase name}}Policy {
  // TODO: Define policy fields
  threshold: number;
}

/**
 * Input for {{camelCase name}} calculation
 */
export interface {{pascalCase name}}Input {
  // TODO: Define your input fields
  id: string;
  data: Record<string, unknown>;
  policy?: Partial<{{pascalCase name}}Policy>;
}

/**
 * Internal context for {{camelCase name}} pipeline
 */
export interface {{pascalCase name}}Context extends OperationContext {
  // Inputs (spread from {{pascalCase name}}Input)
  id: string;
  data: Record<string, unknown>;
  policy: {{pascalCase name}}Policy;

  // Pipeline outputs (populated by operations)
  reasonCodes: string[];
  score?: number;

  // TODO: Add operation-specific output fields
}

/**
 * Result from {{camelCase name}} calculation
 */
export interface {{pascalCase name}}Result {
  score?: number;
  reasonCodes: string[];
  metadata?: Record<string, unknown>;
  // TODO: Define result fields
}
