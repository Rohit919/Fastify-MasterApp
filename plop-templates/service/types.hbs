import type { OperationContext } from '@core/orchestration/index.js';

/**
 * {{pascalCase name}} Service Types
 * Generated by: npm run generate
 */

// Input type for creating a {{camelCase name}}
export interface Create{{pascalCase name}}Input {
  // TODO: Define your input fields
  // Example:
  // name: string;
  // description: string;
}

// Result type
export interface {{pascalCase name}} {
  id: string;
  createdAt: Date;
  updatedAt: Date;
  // TODO: Add your fields
}

// Pipeline context
export interface {{pascalCase name}}PipelineContext extends OperationContext {
  input: Create{{pascalCase name}}Input;
  {{camelCase name}}?: {{pascalCase name}};
  validationErrors?: string[];
  // TODO: Add any context-specific fields
}
